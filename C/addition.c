        //INTRODUCTION
#include <stdio.h>

int main() {
    
    int x, y;
    printf("Enter the first number \n");
    scanf("%d", &x);
    printf("Enter the second number \n");
    scanf("%d", &y);
    int sum = x + y;
    printf("%d", sum);
    // We can use data types such as int (interger) and (char) for characters when
    // declaring and storing specific values. By using scanf and printf we can direct input and output values according to the code.
    return 0;
}   
